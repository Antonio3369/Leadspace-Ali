---
description: 代码修改完成后自动启动本地沙箱并打开浏览器验证
alwaysApply: true
---

# 修改后启动本地沙箱

每次完成代码修改（功能、UI、API 等）后，在汇报结果前默认执行：

1. **数据库沙箱**：若 Prisma Dev 未运行，执行 `npx prisma dev -d`（后台）
2. **开发服务器**：若 3000 端口未占用，执行 `npm run dev`（后台）
3. **打开浏览器**：访问 http://localhost:3000 供用户验证（优先打开与本次改动相关的页面）

## 注意

- 仅本地验证，**不自动部署生产**（见 `Leadspace.Ali.md` §15.3）
- 若沙箱或 dev 已在运行，不重复启动
- 若 `npm run build` 是本次改动的必要验证步骤，在启动 dev 前先跑 build
- 启动失败时自行排查（端口占用、Prisma OOM 等）并 retry，完成后说明状态
