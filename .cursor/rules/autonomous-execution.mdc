---
description: 收到明确任务后自主跑完全流程，不逐步等待用户确认
alwaysApply: true
---

# 自主执行，无需逐步确认

当用户已经给出明确指令（如「从 P0 开始搭建」「继续 P1」「帮我启动沙箱」）时：

- **一口气跑完**：安装依赖、写代码、跑命令、修报错、验证构建，直到任务完成或遇到真正无法自行决定的阻塞
- **不要中途停下来问「是否继续」**：用户不应守在电脑前逐步点同意
- **仅在以下情况才打断用户**：
  - 需求存在多种方案且 trade-off 显著，无法从上下文推断偏好
  - 缺少必要信息（如 API 密钥、样例文件、目标分支名）且无法合理默认
  - 操作不可逆或高风险（force push、删库、提交含密钥的文件等）
- **命令与工具**：在任务范围内直接执行，包括 `npm install`、数据库迁移、启动 dev server、多次 retry 修 CI/构建错误
- **完成后**：用简洁摘要汇报做了什么、如何验证、有无遗留项
